/** Amber warning accent reused from alert severity styling (IntelligenceFeed warning). */
export const NEEDS_CATEGORY_ROW_CLASS = "border-l-2 border-l-amber-500";

/** Highlights the category dropdown when a row still needs a category picked. */
export const NEEDS_CATEGORY_SELECT_CLASS =
  "border-amber-500/50 bg-amber-500/5 ring-1 ring-amber-500/25";
